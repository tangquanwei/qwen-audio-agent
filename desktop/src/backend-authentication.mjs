// Kept as a compatibility surface for integrations importing the old module.
export {
  configurationLaunch as terminalLaunch,
  openBackendConfiguration as openBackendAuthentication,
} from './backend-configuration.mjs'
